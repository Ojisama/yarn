name: Run GitHubAction Workflow
on: push
jobs:
  Do Stuff:
    name: Do stuff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: |
          yarn
          yarn build
          yarn lint
